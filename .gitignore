CLAUDE.md

# coldgen build artifacts: the world and its R1/R2 audit reports.
# Regenerated by every `worldtool coldgen`; the committed baselines are the
# fresh-world-*.txt files, refreshed from a verified build.
fresh.ilod
fresh.ilod.unbound-fcells.txt
fresh.ilod.unbound-vcells.txt
